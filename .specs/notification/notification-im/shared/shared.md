# `notification.shared` — 共享出口与 typed props

- **设计源**: [`.design/notification/comment/image.png`](../../../../.design/notification/comment/image.png)
- **父节点**: `notification.shared`
- **职责**: 导出共享组件与公共类型；复用现有时间格式化。

### 组成

| 区域 | 元素 | 说明 |
|------|------|------|
| 出口 | index / 类型 | |
| 时间 | format utils | 已有 `format-time` |

### UI

| 属性 | 规格（Tailwind / 行为） |
|------|-------------------------|
| — | 无独立 UI |

### 交互

| 手势/键 | 条件 | 行为 | 回调 / 状态效果 |
|---------|------|------|-----------------|
| — | — | — | — |

### 状态

- 无

### 边界

- 秒级字符串与毫秒时间戳兼容转换。

### 本期不做

- 业务请求。

### 组件验收

- [ ] 子组件可经统一出口引用
- [ ] 时间格式化可复用现有工具
