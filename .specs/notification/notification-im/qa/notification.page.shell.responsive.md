# QA · `notification.page.shell.responsive`

- **task_id**：`notification.page.shell.responsive`
- **关联 spec**：`.specs/notification/notification-im/page-shell/responsive.md`
- **入口**：/app/notification
- **Mock 依赖**：无

> 给人测：按功能点逐步点 UI / 验交互。勾选「通过」列即可。

## 功能点 1 · 桌面双栏 / 窄屏切换

### 1. 功能点简介

桌面双栏 / 窄屏切换（对齐组件验收）。

### 2. 如何测试

宽屏查看消息 Tab 列表+详情；缩窄视口进入详情后点返回。

### 3. 怎样算通过

- [ ] 桌面双栏无横向溢出
- [ ] 窄屏可列表↔详情往返

