# pre-execution

1. 确认 mode：`create` | `sync-progress` | `iterate`。
2. 确认设计根目录或既有 Spec/任务路径。
3. 提醒：本 Skill 只写 Spec/任务清单；不写业务实现；澄清未完成禁止落盘。
4. 提醒：新 create / 触达的 iterate 须用方案 C 双层（分析 + 实现契约）；分析层自然语言样子，禁止默认灌满 Tailwind。
