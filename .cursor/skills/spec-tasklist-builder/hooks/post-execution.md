# post-execution

按 `schema.json#outputs` 摘要：

1. `status` / `mode`
2. `artifacts`（Spec、任务清单路径）
3. `open_questions`（应为空；非空则 status 不得为 success）
4. `tree_summary`（页面/Tab/组件一行条）
5. `warnings`（重复设计文件、unknown 任务等）
6. `self_check` 勾选结果
