/**
 * @file 全屏 Loading 模块导出
 * @description 布局中挂载 `FullScreenLoadingHost` 一次，业务里 `Loading.show()` / `Loading.hide()`。
 */

export { default as FullScreenLoadingHost } from './full-screen-loading-host.svelte';
export { Loading } from './full-screen-loading.svelte';
