# 发布模块（Release）进度确认

> 对照文档：`docs/产品需求文档.md`（重点核对 `6.2.5 发布`、`6.2.12 图片视频预览编辑器`）  
> 核对范围：`src/routes/app/release`、`src/lib/modules/media-cover`、`src/lib/components/custom/shin-rich`、`src/lib/stores/release/release-draft.ts`  
> 结论基于当前代码实现，不基于口头计划。

## 一、总览结论

- **核心可用**：发布页主链路可跑通（填写 -> 保存/自动保存 -> 确认发布 -> 上传 -> CreatePost）。
- **部分达成**：封面与发布后体验仅完成基础能力，和 PRD 的完整交互还有明显差距。
- **关键缺口**：封面强制裁切、媒体拖拽/编辑、发布后横幅与圆形状态栏、编辑帖子模式、现网用户补齐。

## 二、按 PRD 条目确认

### 2.1 `6.2.5.1` 帖子内容编辑表单

| 需求条目                                                 | 当前状态              | 代码确认                                                      |
| -------------------------------------------------------- | --------------------- | ------------------------------------------------------------- |
| 封面比例 `1:1 / 4:3 / 3:4`                               | ✅ 已实现             | `coverRatio` + `rotateCoverRatio`，`ReleaseCoverPreview` 展示 |
| 未设封面时使用第 1 张图片/视频首帧；无媒体时正文生成封面 | ✅ 已实现（自动策略） | `resolveCoverBlob` 已覆盖图片/视频首帧/文字封面               |
| “只能选择 1 张图片作为封面 + 强制弹出编辑器裁切”         | ❌ 未实现             | 当前没有“独立封面选择并强制裁切”流程                          |
| 选择图片/视频最多 20 张                                  | ✅ 已实现             | `MAX_MEDIA_COUNT = 20`                                        |
| 标题最大 20 字                                           | ✅ 已实现             | `Input maxlength={20}` + 计数                                 |
| 正文最大 10000 字                                        | ✅ 已实现             | `ShinRichTextarea maxLength` 默认 10000                       |
| `@` 用户：插入、popover、上下键、enter                   | ✅ 已实现             | `ShinRichTextarea` + `RichTextareaController`                 |
| `@` 后按 QQ/昵称/备注查询                                | ✅ 已实现             | `filterUsersByQuery`                                          |
| 优先关注列表，不足 20 人补现网用户                       | ⚠️ 部分实现           | 关注列表已接；补现网用户仍是 TODO                             |
| 分区选择必填                                             | ✅ 已实现             | `selectedSection` 校验 + 发布前阻断                           |
| 校验：封面/媒体/正文任一可提交                           | ⚠️ 部分实现           | 当前是“标题/正文/媒体任一”；独立封面输入不存在                |

### 2.2 `6.2.5.2` 操作区

| 需求条目                        | 当前状态    | 代码确认                               |
| ------------------------------- | ----------- | -------------------------------------- |
| 重置 + 二次确认                 | ⚠️ 部分实现 | 新建场景可清空；编辑场景“恢复现网”未做 |
| 保存（无变更提示/有变更持久化） | ✅ 已实现   | `isDirty` + `saveReleaseDraft`         |
| 每 1 分钟自动保存               | ✅ 已实现   | `setInterval(..., 60000)`              |
| “保存于/自动保存于 xx:xx”       | ✅ 已实现   | `formatTimeAccuracyFirst`              |
| 发布帖子 + 二次确认             | ✅ 已实现   | `showPublishConfirm` + `handleSubmit`  |

### 2.3 `6.2.5.3` 意外兜底

| 需求条目            | 当前状态  | 代码确认                                  |
| ------------------- | --------- | ----------------------------------------- |
| 路由跳转前二次确认  | ✅ 已实现 | `beforeNavigate`                          |
| 关闭标签页/刷新提醒 | ✅ 已实现 | `beforeunload`                            |
| 退出前自动保存      | ✅ 已实现 | `handleLeaveConfirm -> performSave(true)` |

### 2.4 `6.2.5.4` 发布以后

| 需求条目                         | 当前状态          | 代码确认                                                                        |
| -------------------------------- | ----------------- | ------------------------------------------------------------------------------- |
| 确认发布后才开始上传             | ✅ 已实现         | `handleSubmit` 中上传后再 `doCreatePost`                                        |
| 上传进度可视化                   | ✅ 已实现         | 底部进度条 +「上传中 x/y」+ Uppy `progress` 事件                                |
| 取消上传                         | ✅ 已实现         | `onCancelUpload` + `cancelAll`                                                  |
| 成功/失败提示                    | ✅ 已实现（基础） | toast 已有                                                                      |
| 上传横幅通知（含封面/状态/进度） | ❌ 未实现         | 仅有 TODO                                                                       |
| 隐藏横幅 -> 右下圆形状态栏       | ❌ 未实现         | 仅有 TODO                                                                       |
| 多任务叠加展示                   | ❌ 未实现         | 仅有 TODO                                                                       |
| 断线失败后的重试/删除            | ✅ 已实现         | `handleRetryUpload` + `handleRemoveFailedAndProceed`，`retryAll` / `removeFile` |
| Webview 保活                     | ❌ 未实现         | 仅有 TODO，且依赖原生桥接                                                       |
| 发布成功跳转帖子详情             | ⚠️ 部分实现       | 现在固定 `goto('/')`，未跳帖子详情                                              |

### 2.5 `6.2.8.4` 编辑帖子复用发布页

| 需求条目                              | 当前状态  | 代码确认                         |
| ------------------------------------- | --------- | -------------------------------- |
| 进入编辑模式、回填现网、调用 EditPost | ❌ 未实现 | 当前仅新建发布流程（CreatePost） |

## 三、已确认的基建能力

| 模块               | 状态 | 说明                                              |
| ------------------ | ---- | ------------------------------------------------- |
| `release-media`    | ✅   | `File[] <-> DraftMediaItem[]`，含 Live Photo 解析 |
| `media-uploader`   | ✅   | Uppy + 分片上传能力已接入                         |
| `release-draft`    | ✅   | IndexedDB 持久化、schemaVersion 与默认值填充      |
| `media-cover`      | ✅   | 图片优先、视频首帧、文字封面统一决策              |
| `ShinRichTextarea` | ✅   | 富文本输入、@ 提及、内容提取                      |

## 四、阻塞与依赖

| 事项                               | 依赖方                           | 当前情况         |
| ---------------------------------- | -------------------------------- | ---------------- |
| 独立封面强制裁切（选择后立即编辑） | 图片/视频编辑器能力              | 依赖编辑器接入   |
| `@` 用户补足“现网用户”             | 后端用户搜索接口                 | 接口未就绪       |
| 发布后横幅/圆形状态栏              | 全局通知组件方案                 | 尚未接入         |
| Webview 保活                       | 原生壳层/桥接                    | 前端无法单独落地 |
| 编辑帖子模式                       | 帖子详情入口 + EditPost 流程联调 | 尚未开始         |

## 五、建议的下一步（按优先级）

1. **P1**：补“发布成功跳转帖子详情”、补“编辑场景重置恢复现网”。
2. **P1**：实现媒体缩略图长按/右键 popover（编辑/删除）与拖拽排序。
3. **P2**：接入封面强制裁切流程（选择后立即进入编辑器）。
4. **P2**：接入统一上传横幅 + 圆形状态栏 + 多任务叠加。
5. **P3**：等后端接口后补“现网用户”候选，等原生桥接后补 Webview 保活。

---

_最后更新：2026-03-14（按当前代码核对）_
