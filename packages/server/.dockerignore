# ────────────────────────────────────────────────────────────────────────────
# .dockerignore
# 排除不需要进入 Docker 构建上下文的文件/目录。
# 好处：
#   1. 减少 docker build 发送到 daemon 的数据量（构建上下文体积）
#   2. 防止本地敏感文件意外进入镜像
#   3. 避免本地 .git 变更触发不必要的缓存失效
# ────────────────────────────────────────────────────────────────────────────

# Git 仓库元数据（最大的无用数据源，动辄数十 MB）
.git
.gitignore
.gitattributes

# 文档和说明文件（运行时不需要）
*.md
doc/
README*

# 测试文件（构建生产镜像不需要）
test/
*_test.go

# IDE / 编辑器配置
.vscode/
.idea/
*.code-workspace
.cursor/

# 本地开发工具生成的文件
*.out
*.test
*.prof

# 本地 Docker 相关（避免递归包含）
.dockerignore
docker-compose*.yml

# 本地日志
logs/
*.log

# 脚本目录（只在宿主机执行，不进入镜像）
script/
