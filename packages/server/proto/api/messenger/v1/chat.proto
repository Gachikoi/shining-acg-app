syntax = "proto3";

package api.messenger.v1;

import "api/common/v1/common.proto";

// -------------------------------------------------------
// 私信 (Chat) 服务 - [暂时挂起/开发阶段暂不做]
// -------------------------------------------------------
//service ChatService {
  // 获取会话列表
  // rpc ListConversations(ListConversationsRequest) returns (ListConversationsResponse);

  // 获取聊天记录
  // rpc ListMessages(ListMessagesRequest) returns (ListMessagesResponse);

  // 发送私信
  // rpc SendMessage(SendMessageRequest) returns (SendMessageResponse);

  // ==========================================
  // 私信模型 (已注释 - 待后续开发)
  // ==========================================
//}

/*
enum MessageType {
  MESSAGE_TYPE_TEXT = 0;
  MESSAGE_TYPE_IMAGE = 1;
  MESSAGE_TYPE_VIDEO = 2;
  MESSAGE_TYPE_AUDIO = 3;
}

message Message {
  string message_id = 1;
  string sender_id = 2;
  string receiver_id = 3;
  MessageType type = 4;
  string content = 5;
  api.common.v1.Media media = 6; // 建议复用 Common Media
  int64 sent_at = 7;
  bool is_read = 8;
}

message Conversation {
  string conversation_id = 1;
  api.common.v1.UserSummary peer = 2;
  Message last_message = 3;
  int32 unread_count = 4;
  int64 updated_at = 5;
}

message ListConversationsRequest {
  api.common.v1.Pagination pagination = 1;
}

message ListConversationsResponse {
  repeated Conversation conversations = 1;
  string next_page_token = 2;
}

message ListMessagesRequest {
  string conversation_id = 1; // 建议用 conversation_id 而不是 peer_id
  api.common.v1.Pagination pagination = 2;
}

message ListMessagesResponse {
  repeated Message messages = 1;
  string next_page_token = 2;
}

message SendMessageRequest {
  string receiver_id = 1;
  string content = 2;
  MessageType type = 3;
}

message SendMessageResponse {
  Message message = 1;
}
*/